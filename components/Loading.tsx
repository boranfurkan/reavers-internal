import React from "react";

function Loading() {
  return (
    <div className="absolute inset-0 z-[222] flex h-screen w-full items-center justify-center bg-reavers-bg bg-opacity-80 backdrop-blur-lg">
      Loading
    </div>
  );
}

export default Loading;
